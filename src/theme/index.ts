import { globalDefaultStyles } from './styles';

import {
  styled,
  css,
  globalCss,
  keyframes,
  getCssText,
  configTheme,
  theme,
} from './stitches';

export {
  configTheme,
  theme,
  styled,
  css,
  globalCss,
  keyframes,
  getCssText,
  globalDefaultStyles,
};
